apiVersion: v1
kind: Service
metadata:
  name: storybook-svc
  labels:
    app: storybook-app
spec:
  selector:
    app: storybook-app
  ports:
    - protocol: TCP
      port: 6007
      targetPort: 6007
  type: LoadBalancer
