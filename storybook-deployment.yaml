apiVersion: apps/v1
kind: Deployment
metadata:
  name: storybook-dep
  labels:
    app: storybook
spec:
  replicas: 1
  selector:
    matchLabels:
      app: storybook-app
  template:
    metadata:
      labels:
        app: storybook-app
    spec:
      containers:
        - name: storybook
          image: rafcasto/techdojo-ui-component:9
          ports:
            - containerPort: 6006
